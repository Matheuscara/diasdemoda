name = "diasdemoda"
compatibility_date = "2026-08-20"

# Site 100% estático servido pelo Cloudflare Workers (Static Assets).
# O "output directory" mora aqui, não na tela de setup.
[assets]
directory = "./dist"
